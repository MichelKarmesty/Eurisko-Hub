{
  "name": "eurisko-hub",
  "version": "0.1.0",
  "private": true,
  "description": "Eurisko Hub - Internal Operations Service Hub. Repository-root entry points; the app itself lives in backend/ (NestJS API) and frontend/ (React + Vite).",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "start": "node scripts/dev.mjs",
    "test": "node scripts/run-tests.mjs"
  }
}
